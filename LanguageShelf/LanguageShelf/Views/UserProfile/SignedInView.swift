import SwiftUI

struct SignedInView: View {
    var body: some View {
        ZStack {
            Color("BackgroundColor")
            
            Text("Signed in user")
        }
    }
}

#Preview {
    SignedInView()
}
